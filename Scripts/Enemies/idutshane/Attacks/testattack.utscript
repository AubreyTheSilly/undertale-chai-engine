initvar(timer,NUMBER,0);

while (timer <= 30) {
    print(timer);
    if (mod(timer,10) == 0) {createAttack("attack{timer}",vec2(0,-50),loadTexture("spr_heartsmall_0"),vec2(0,2),"blue",false);};

    timer += 1;

    await process_frame;
};